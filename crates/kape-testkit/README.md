# kape-testkit

Reusable contract checks for `Kape` backend adapters.

The suite covers scalar and batch miss/hit behavior, immortal and expiring TTL,
ordered duplicate keys, fresh existence checks, and removal.

This crate is an internal workspace test utility and is not published.
